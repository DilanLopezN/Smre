import { styled } from 'styled-components';

export const TableContainer = styled.div`
  .editable-cell {
    position: relative;
  }

  .editable-cell-value-wrap {
    padding: 5px 0px;
    cursor: pointer;
  }
`;
